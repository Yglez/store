import React, { Component } from "react";

export default class Default extends Component {
  render() {
    return (
      <React.Fragment>
        <div>Default</div>
      </React.Fragment>
    );
  }
}
